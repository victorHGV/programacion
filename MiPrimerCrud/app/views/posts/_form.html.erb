<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :publicacion %><br>
    <%= f.date_select :public %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
